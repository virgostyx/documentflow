<div class="flex items-center justify-between gap-3">
  <h2 class="text-lg font-semibold text-gray-900">Worker <%= worker.id %> — <%= worker.name %></h2>
  <span class="font-mono text-sm text-gray-500"><%= worker.hostname %></span>
</div>
