<div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <%= render Ui::PageHeaderComponent.new(
    title: "Edit circuit template",
    description: current_entity.name,
    back_path: entity_circuit_templates_path(current_entity),
    back_text: "Circuit templates"
  ) %>

  <%= render Ui::CardComponent.new do %>
    <%= render "form", circuit_template: @circuit_template %>
  <% end %>
</div>
