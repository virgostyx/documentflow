<div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <%= render Ui::PageHeaderComponent.new(
    title: "New distribution list",
    description: current_entity.name,
    back_path: entity_distribution_lists_path(current_entity),
    back_text: "Distribution lists"
  ) %>

  <%= render Ui::CardComponent.new do %>
    <%= render "form", distribution_list: @distribution_list %>
  <% end %>
</div>
